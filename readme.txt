Starting a go project
    1. create a project folder with desired name and cd into that folder
    2. then use command "go mod init projectname" to create a mod file which will manage the project
    3. then create a file with .go extension and using the command "go run filename.go"
    4.  we can also use "go build" to build and the run the builded file